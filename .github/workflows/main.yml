name: imagemap-tools build
on : 
  push:
    branches:
      - master

jobs :
    build:
      name: react build
      runs-on: self-hosted 
      
      steps:
          - uses: actions/checkout@master
          - name: checkout
            working-directory: ~/productions

        

